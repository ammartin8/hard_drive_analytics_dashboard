hard_drive_data:
  outputs:
    dev:
      dataset: hard_drive_dataset
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile: ../.google/credentials/google_credentials.json
      location: US
      method: service-account
      priority: interactive
      project: my-project-id # Add project ID here
      threads: 2
      type: bigquery
  target: dev